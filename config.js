const config = {
    host: "localhost",
    port: 8889, // 3306 for windows/linux/newer macs
    uname: "root",
    pword: "root", // blank for windows
    dbase: "smokestats"
}

module.exports = config;